// 백준 9996번 한국이 그리울땐 서버에 접속하지
// 첫번째 줄에 파일의 개수, 두번째에 파일 패턴(a*d) 

#include<bits/stdc++.h>
using namespace std;

int n;
string s, pa;
int main() {
	cin >> n;
	for (int i = 0; i < n; i++) {
		getline(cin, pa);
		getline(cin, s);
		for (int j = 0; j < s.size(); j++) {

		}
	}
	
}